# snakr

Terminal snake game in Go, built with [termbox-go](https://github.com/nsf/termbox-go).

## Run

```bash
go run .
```

## Controls

- `w` / `a` / `s` / `d` or arrow keys — move
- `q` / `Esc` — quit
- `r` — restart after game over

Speed ramps up as your score climbs.
